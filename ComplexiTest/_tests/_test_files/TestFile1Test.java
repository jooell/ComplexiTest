package org.apache.commons.math4.legacy.distribution;

import org.apache.commons.math4.legacy.distribution.AbstractIntegerDistribution;
import org.apache.commons.statistics.distribution.DiscreteDistribution;
import org.junit.Test;
import static org.junit.Assert.*;

public class TestFile1Test {


    // Returns an array of size n when given a valid number of samples and sampler.
    @ParameterizedTest
    public void test_valid_number_of_samples() {
        while (true) {
            System.out.println("hej");
            // a comment
            if(true) {
                System.out.println("halloj");
                if(true) {
                   System.out.println("halloj");
                }
            }
        }
        var x = new Ball(1);
        assertEquals(n, result.length);
    }

    // Returns an array of size n when given a valid number of samples and sampler.
    @Test
    public void test_valid_number_of_samples() {
        while (true) {
            System.out.println("hej");
        }
        /**
        Another comment.
        */
        var x = new Ball(1);
        assertEquals(n, result.length);
        assertTrue(true);
    }

    private void non_test_method() {
        while (true) {
            var x = 1;
        }
    }
}